## задание
- есть реляционная БД MySQL со схемой (страна-город, язык по стране).
- Необходимо вынести все данные, которые запрашиваются часто, в Redis

## Перечень необходимого установленного ПО

* **IntelliJ IDEA Ultimate**
* **Java 18**
* **DOCKER**
* **Workbench**  (либо любой другой сервис для администрирования mySql)

## использовано
- MAVEN
- liquibase
- hibernate
- p6spy
- mySql
- Redis
- H2DB
- Lombok


## реализовано
- через liquibase поднята БД MySQL в контейнере Docker
- ORM БД  через Hibernate
- сервис получения тестовых данных из mySQL
- сервис записи данных в Redis
- сервис получения тестовых данных из Redis
- подключение к H2DB
- создание таблиц с данными в H2DB
- получение тестовых данных из H2DB
- реализовано сравнение времени выполнения запросов в  mySQL, Redis, H2DB




## особенности реализации
- при запуске проекта проверяется наличие необходимой схемы БД в mySql,  при ее отсутсвии liquibase выполняет скрипт ее создания.

## запуск на локальной машине
- запустить  Docker
- стартовать контейнер mySQL
- стартовать контейнер Redis 
- RUN

## отчет
- результат тестирования выводится в консоль.
- в виде (пример)
  Redis:	173 ms
  MySQL:	735 ms
  H2DB:	638 ms


